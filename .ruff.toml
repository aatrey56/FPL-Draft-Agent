target-version = "py311"
line-length = 120

# Always skip virtual-environment directories to prevent ruff from traversing
# thousands of installed-package files when invoked from the repo root.
exclude = [
  ".venv",
  "*/.venv",
  "**/.venv/**",
]

[lint]
select = ["E9", "F63", "F7", "F82"]
